name: Summer me!

on: 
  issues: 
    types: [opened, labeled]
  
jobs:
  summer-me-profile-pic:
    runs-on: ubuntu-latest
    if: ${{ github.event.label.name == 'summer-me' }}
    steps:
      - run: echo "123"
  
